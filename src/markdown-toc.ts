import * as vscode from 'vscode';
import * as ext_helper from './extension-helper';

export function insertToc(depthFrom: number, depthTo: number) {
    console.info('markdown-toc.insertToc');
}

export function deleteToc(depthFrom: number, depthTo: number) {
    console.info('markdown-toc.deleteToc');
}