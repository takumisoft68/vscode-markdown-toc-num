import * as vscode from 'vscode';
import * as ext_helper from './extension-helper';

export function insertChapterNumber(chapterDepthFrom: number, chapterDepthTo: number) {
    console.info('markdown-toc.insertChapterNumber');
}

export function deleteChapterNumber(chapterDepthFrom: number, chapterDepthTo: number) {
    console.info('markdown-toc.deleteChapterNumber');
}
